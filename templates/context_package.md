# Context Package — [Agent ID] — [YYYY-MM-DD]

> *This is the template for context packages assembled by HELM before each agent invocation. Context packages are written to `/context/[user]/YYYY-MM-DD-[agent_id].md` and versioned in git. See [SETUP.md §13](../SETUP.md#13-context-scoping) for the full context scoping specification.*

---

**Agent:** [agent_id]
**Flow:** [flow_id — e.g., `am_sweep`, `overnight_triage`, `transit_prep`, `time_block`]
**Date:** [YYYY-MM-DD]
**User:** [username]
**Token Count:** [estimated tokens] / [max tokens — default 20,000]
**Version:** [package version — incremented if regenerated within same day]
**Generated:** [ISO 8601 timestamp]

---

## Context Keys

> *Only the keys declared in `agents.yaml` → `context_keys` for this agent are included. Each key is populated from the source listed below. If a key exceeds the token limit, it is summarized automatically — the full content remains available in the repo. See [SETUP.md §13](../SETUP.md#13-context-scoping) for key definitions.*

### `tasks_today`

> *Source: Todoist API + TASK_CLASSIFIER output. Full list of today's classified tasks with metadata. Used by: `email_drafter`, `time_blocker`.*

```
[Populated at runtime — classified task list in task_object format]
```

---

### `calendar_today`

> *Source: Google Calendar API. Today's events + next 24h. Used by: `calendar_manager`, `task_classifier`.*

```
[Populated at runtime — calendar events with times, locations, attendees]
```

---

### `recent_transcripts`

> *Source: Zoom API. Last 3 Zoom meeting summaries (AM Sweep) or last 24h (Overnight Triage). Used by: `task_classifier`, `notes_agent`.*

```
[Populated at runtime — meeting summaries with action items]
```

---

### `voice_profile`

> *Source: `/users/[user]/VOICE.md`. Full contents. Used by: `email_drafter`, `notes_agent`.*

```
[Populated at runtime — full VOICE.md contents]
```

> *⚠ If this key appears truncated or summarized, flag it in the agent's completion report. A truncated voice profile degrades draft quality.*

---

### `client_profiles`

> *Source: `/users/[user]/CLIENTS.md`. Full contents including Do Not Touch list. Used by: all agents.*

```
[Populated at runtime — full CLIENTS.md contents]
```

> *⚠ If truncated, agents must not draft for contacts whose profiles may be missing.*

---

### `scheduling_rules`

> *Source: `/users/[user]/SCHEDULING.md`. Protected blocks, meeting rules, task routing. Used by: `calendar_manager`, `time_blocker`, `task_classifier`.*

```
[Populated at runtime — full SCHEDULING.md contents]
```

---

### `classification_rules`

> *Source: `/users/[user]/CLASSIFY.md`. Classification logic, source-specific rules. Used by: `task_classifier`.*

```
[Populated at runtime — full CLASSIFY.md contents]
```

---

### `user_profile`

> *Source: `/users/[user]/USER.md`. Identity, working hours, locations, hard rules. Used by: `task_classifier`.*

```
[Populated at runtime — full USER.md contents]
```

---

### `user_locations`

> *Source: Location list extracted from USER.md. Used by: `calendar_manager`, `time_blocker`.*

```
[Populated at runtime — location names and addresses]
```

---

### `working_hours`

> *Source: Working hours section from USER.md. Used by: `time_blocker`, `calendar_manager`.*

```
[Populated at runtime — working hours per day]
```

---

### `research_request`

> *Source: Specific task being researched. Used by: `research_agent`.*

```
[Populated at runtime — research task details, scope, deliverable format]
```

---

### `existing_notes`

> *Source: `/knowledge/[user]/clients/[client].md`. Current content of the client's knowledge base file. Used by: `notes_agent`.*

```
[Populated at runtime — existing knowledge base content for the relevant client]
```

---

## Token Budget

| Key | Tokens | % of Budget |
|-----|--------|-------------|
| [key_name] | [count] | [percentage] |
| **Total** | **[total]** | **[total %]** |

> *If total exceeds the configured cap (default: 20,000 tokens), keys are summarized in reverse priority order. Full content always available in the repo for reference.*

---

## Debug Info

| Field | Value |
|-------|-------|
| Package version | [n] |
| Generated by | HELM [version] |
| Engine | [engine_id from engine.yaml] |
| Flow run ID | [unique run identifier] |
| Summarized keys | [list or "none"] |
| Missing keys | [list or "none"] |
| Warnings | [list or "none"] |
